#set your language below
language: java

jdk:
  - oraclejdk8

   
build:
  cache:
  cache_dir_list:
    - /root/src/github.com/.m2

  ci:
    - ls -la /root/src/github.com/
    - echo $M2_HOME
    - mvn -X --version
    - mvn -X help:evaluate -Dexpression=settings.localRepository
